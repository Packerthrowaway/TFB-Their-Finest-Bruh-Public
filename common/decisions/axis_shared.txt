AXIS_propaganda_campaigns = {
	AXIS_together_prop = { # ID = 2
		priority = 2

		icon = ger_propaganda_campaigns

		allowed = {
			OR = {
				original_tag = FIN
				original_tag = ROM
				original_tag = HUN
				original_tag = BUL
				original_tag = YUG
				original_tag = SPR
				original_tag = VIC
				original_tag = SIA
			}
		}

		available = {
			NOT = {
				has_country_flag = AXIS_running_prop
			}
		}

		days_remove = 365

		days_re_enable = 360

		cost = 50

		modifier = {
			research_sharing_per_country_bonus_factor = 0.25
			command_power_gain_mult = 0.125
			autonomy_gain = 0.25
		}

		complete_effect = {
			set_variable = { AXIS_poster_center_icon_frame = 2 }
			set_country_flag = AXIS_running_prop
		}

		remove_effect = {
			set_variable = { AXIS_poster_center_icon_frame = 1 }
			clr_country_flag = AXIS_running_prop
		}
	}

	AXIS_women_prop = { # ID = 3
		priority = 2

		icon = ger_propaganda_campaigns

		allowed = {
			OR = {
				original_tag = FIN
				original_tag = ROM
				original_tag = HUN
				original_tag = BUL
				original_tag = YUG
				original_tag = SPR
				original_tag = VIC
				original_tag = SIA
			}
		}

		available = {
			NOT = {
				has_country_flag = AXIS_running_prop
			}
		}

		days_remove = 365

		days_re_enable = 360

		cost = 50

		modifier = {
            industrial_capacity_factory = 0.025
            production_lack_of_resource_penalty_factor = -0.1
		}

		complete_effect = {
			set_variable = { AXIS_poster_center_icon_frame = 3 }
			set_country_flag = AXIS_running_prop
		}

		remove_effect = {
			set_variable = { AXIS_poster_center_icon_frame = 1 }
			clr_country_flag = AXIS_running_prop
		}
	}

	AXIS_finest_hour_prop = { # ID = 4
		priority = 2

		icon = ger_propaganda_campaigns

		allowed = {
			OR = {
				original_tag = FIN
				original_tag = ROM
				original_tag = HUN
				original_tag = BUL
				original_tag = YUG
				original_tag = SPR
				original_tag = VIC
				original_tag = SIA
			}
		}

		available = {
			NOT = {
				has_country_flag = ENG_running_prop
			}
		}

		days_remove = 365

		days_re_enable = 360

		cost = 50

		modifier = {
            industry_air_damage_factor = -0.125
            static_anti_air_hit_chance_factor = 0.05
            industry_repair_factor = 0.125
		}

		complete_effect = {
			set_variable = { AXIS_poster_center_icon_frame = 4 }
			set_country_flag = AXIS_running_prop
		}

		remove_effect = {
			set_variable = { AXIS_poster_center_icon_frame = 1 }
			clr_country_flag = AXIS_running_prop
		}
	}

	AXIS_careless_talk_prop = { # ID = 5
		priority = 2

		icon = ger_propaganda_campaigns

		allowed = {
			OR = {
				original_tag = FIN
				original_tag = ROM
				original_tag = HUN
				original_tag = BUL
				original_tag = YUG
				original_tag = SPR
				original_tag = VIC
				original_tag = SIA
			}
		}

		available = {
			NOT = {
				has_country_flag = AXIS_running_prop
			}
		}

		days_remove = 365

		days_re_enable = 360

		cost = 50

		modifier = {
            experience_loss_factor = -0.05
            experience_gain_army_factor = 0.05
		}

		complete_effect = {
			set_variable = { AXIS_poster_center_icon_frame = 5 }
			set_country_flag = AXIS_running_prop
		}

		remove_effect = {
			set_variable = { AXIS_poster_center_icon_frame = 1 }
			clr_country_flag = AXIS_running_prop
		}
	}

	AXIS_wings_victory_prop = { # ID = 6
		priority = 2

		icon = ger_propaganda_campaigns

		allowed = {
			OR = {
				original_tag = FIN
				original_tag = ROM
				original_tag = HUN
				original_tag = BUL
				original_tag = YUG
				original_tag = SPR
				original_tag = VIC
				original_tag = SIA
			}
		}

		available = {
			NOT = {
				has_country_flag = AXIS_running_prop
			}
		}

		days_remove = 365

		days_re_enable = 360

		cost = 50

		modifier = {
            research_sharing_per_country_bonus_factor = 0.25
            command_power_gain_mult = 0.5
            offensive_war_stability_factor = 0.05
		}

		complete_effect = {
			set_variable = { AXIS_poster_center_icon_frame = 6 }
			set_country_flag = AXIS_running_prop
		}

		remove_effect = {
			set_variable = { AXIS_poster_center_icon_frame = 1 }
			clr_country_flag = AXIS_running_prop
		}
	}

	AXIS_dig_victory_prop = { # ID = 7
		priority = 2

		icon = ger_propaganda_campaigns

		allowed = {
			OR = {
				original_tag = FIN
				original_tag = ROM
				original_tag = HUN
				original_tag = BUL
				original_tag = YUG
				original_tag = SPR
				original_tag = VIC
				original_tag = SIA
			}
		}

		available = {
			NOT = {
				has_country_flag = AXIS_running_prop
			}
		}

		days_remove = 365

		days_re_enable = 360

		cost = 50

		modifier = {
            industry_free_repair_factor = 0.125
            industry_repair_factor = 0.125
            production_speed_infrastructure_factor = 0.075
            production_speed_rail_way_factor = 0.075
		}

		complete_effect = {
			set_variable = { AXIS_poster_center_icon_frame = 7 }
			set_country_flag = AXIS_running_prop
		}

		remove_effect = {
			set_variable = { AXIS_poster_center_icon_frame = 1 }
			clr_country_flag = AXIS_running_prop
		}
	}
}